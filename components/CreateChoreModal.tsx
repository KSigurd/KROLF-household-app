import React, { FC } from "react";
import { View, Text } from "react-native";

const CreateChoreModal: FC = () => {
    return (
      <View>
        <Text>CreateChoreModal</Text>
      </View>
    );
  };
  
  export default CreateChoreModal;